$("ul.errors").html("");
<% if @subscription_type.errors.any? %>
  <% @subscription_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".subscription-types").html("<%= escape_javascript(render 'index') %>");
  $("#subscription-type-modal").modal("hide");
 
<% end %>